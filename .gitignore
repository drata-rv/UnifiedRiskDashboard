# Runtime token config — never commit
tokens.json
tokens.azure.json
tokenList.txt
*.env

# Local SQLite cache/audit-log
data/
*.db
*.db-wal
*.db-shm

# prototype design reference (screenshots + Drata DOM captures — not app code)
prototype files/

# Reference / planning docs — not needed to run the script
PROJECT_SUMMARY.md
Transcript*.txt
*.xlsx
V2_API_swagger.json
drata_swagger_v1.json

# Output files (regenerated by script)
dashboard*.xlsx

# macOS
.DS_Store
__pycache__/
*.pyc
*.pyo
.venv*/
venv/

# Local dev tooling config
.claude/
